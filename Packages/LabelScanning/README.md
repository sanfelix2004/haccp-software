# LabelScanning

Package unificato per scansione etichette HACCP (contratto + motore V2).

## Prodotti

- **LabelScanningContract** — `LabelScanningEngine` + `LabelScanResult`
- **LabelScannerV2** — OCR Vision + Foundation Models + fallback regex

## Test

```bash
cd Packages/LabelScanning
swift test
```
